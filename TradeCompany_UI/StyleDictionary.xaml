<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button" x:Key="ButtonNavigation">
        <Style.Setters>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="Width" Value="105"/>
            <Setter Property="BorderBrush" Value="#F9F9F9"/>
            <Setter Property="Background" Value="#F9F9F9"/>
        </Style.Setters>
    </Style>

    <Style TargetType="Image" x:Key="ButtonNavigationIcons">
        <Setter Property="Margin" Value="0,4,0,0"/>
        <Setter Property="Width" Value="36"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ButtonNavigationText">
        <Style.Setters>
            <Setter Property="Margin" Value="0,3,0,0"/>
            <Setter Property="TextBlock.FontWeight" Value="500"/>
            <Setter Property="TextBlock.FontSize" Value="18"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style.Setters>
    </Style>

</ResourceDictionary>